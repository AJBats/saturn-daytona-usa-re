/* name_entry_render -- L3 assembly (SH-2 mnemonics)
 * Translation unit: 0x0603D518 - 0x0603D5D0
 * Auto-generated by tools/generate_l3_tu.py
 *
 * Configures display scroll fields for the name entry screen.
 * Updates 4 packed config words at scroll_cfg_a (offsets 0, 2, 4, 6),
 * applying byte-1 mask (0xFF00) and byte-0 mask (0x00FF) to each,
 * with a bitfield commit call (sym_06034F78) between each update.
 * Word at offset 4 is zeroed outright instead of masked.
 *
 * Args: r6 = display value to commit via bitfield utility
 */

    .section .text.FUN_0603D518


    .global name_entry_render
    .type name_entry_render, @function
name_entry_render:
    sts.l pr, @-r15                     ! save return address
    mov r6, r0                          ! r0 = display value (arg)
    mov.l   .L_mask_byte1, r7           ! r7 = 0x0000FF00 (byte-1 mask)
    mov.l   .L_pool_config_byte_ptr, r5 ! r5 = config byte pointer (sym_060A4D58)
    mov.l   .L_pool_scroll_cfg_a, r4    ! r4 = scroll config block A (sym_060A4D3E)
    mov.w   .L_bitfield_pos_count, r1       ! r1 = 0x0401 (bit position | count)
    mov.l   .L_pool_fn_bitfield_commit, r3 ! r3 = bitfield read-modify-write fn
    jsr @r3                             ! call bitfield commit(r0=val, r1=pos, r2=target)
    mov r5, r2                          ! r2 = config byte pointer (delay slot)
    mov r6, r0                          ! r0 = display value again
    mov.w @r4, r2                       ! r2 = word[0] from scroll config A
    extu.w r2, r2                       ! zero-extend to 32 bits
    and r7, r2                          ! keep only byte 1 (mask 0xFF00)
    mov.w r2, @r4                       ! write back masked word[0]
    mov.w   DAT_0603d5be, r14           ! r14 = 0x00FF (byte-0 mask)
    mov.w   .L_bitfield_pos_count, r1       ! r1 = 0x0401 (bit position | count)
    mov.l   .L_pool_fn_bitfield_commit, r3 ! r3 = bitfield read-modify-write fn
    jsr @r3                             ! call bitfield commit
    mov r5, r2                          ! r2 = config byte pointer (delay slot)
    mov r6, r0                          ! r0 = display value
    mov.w @r4, r2                       ! r2 = word[0] from scroll config A
    extu.w r2, r2                       ! zero-extend to 32 bits
    and r14, r2                         ! keep only byte 0 (mask 0x00FF)
    mov.w r2, @r4                       ! write back masked word[0]
    mov.w   .L_bitfield_pos_count, r1       ! r1 = 0x0401 (bit position | count)
    mov.l   .L_pool_fn_bitfield_commit, r3 ! r3 = bitfield read-modify-write fn
    jsr @r3                             ! call bitfield commit
    mov r5, r2                          ! r2 = config byte pointer (delay slot)
    mov.w @(2, r4), r0                  ! r0 = word[1] from scroll config A
    mov r0, r2                          ! r2 = word[1]
    extu.w r2, r2                       ! zero-extend to 32 bits
    and r7, r2                          ! keep only byte 1 (mask 0xFF00)
    extu.w r2, r0                       ! zero-extend result
    mov.w r0, @(2, r4)                  ! write back masked word[1]
    mov r6, r0                          ! r0 = display value
    mov.w   .L_bitfield_pos_count, r1       ! r1 = 0x0401 (bit position | count)
    mov.l   .L_pool_fn_bitfield_commit, r3 ! r3 = bitfield read-modify-write fn
    jsr @r3                             ! call bitfield commit
    mov r5, r2                          ! r2 = config byte pointer (delay slot)
    mov.w @(2, r4), r0                  ! r0 = word[1] from scroll config A
    mov r0, r2                          ! r2 = word[1]
    extu.w r2, r2                       ! zero-extend to 32 bits
    and r14, r2                         ! keep only byte 0 (mask 0x00FF)
    extu.w r2, r0                       ! zero-extend result
    mov.w r0, @(2, r4)                  ! write back masked word[1]
    mov r6, r0                          ! r0 = display value
    mov.w   .L_bitfield_pos_count, r1       ! r1 = 0x0401 (bit position | count)
    mov.l   .L_pool_fn_bitfield_commit, r3 ! r3 = bitfield read-modify-write fn
    jsr @r3                             ! call bitfield commit
    mov r5, r2                          ! r2 = config byte pointer (delay slot)
    mov #0x0, r0                        ! r0 = 0
    mov.w r0, @(4, r4)                  ! zero out word[2] of scroll config A
    mov r6, r0                          ! r0 = display value
    mov.w   .L_bitfield_pos_count, r1       ! r1 = 0x0401 (bit position | count)
    mov.l   .L_pool_fn_bitfield_commit, r3 ! r3 = bitfield read-modify-write fn
    jsr @r3                             ! call bitfield commit
    mov r5, r2                          ! r2 = config byte pointer (delay slot)
    mov.w @(6, r4), r0                  ! r0 = word[3] from scroll config A
    mov r0, r2                          ! r2 = word[3]
    extu.w r2, r2                       ! zero-extend to 32 bits
    and r7, r2                          ! keep only byte 1 (mask 0xFF00)
    extu.w r2, r0                       ! zero-extend result
    mov.w r0, @(6, r4)                  ! write back masked word[3]
    mov r6, r0                          ! r0 = display value
    mov.w   .L_bitfield_pos_count, r1       ! r1 = 0x0401 (bit position | count)
    mov.l   .L_pool_fn_bitfield_commit, r3 ! r3 = bitfield read-modify-write fn
    jsr @r3                             ! call bitfield commit
    mov r5, r2                          ! r2 = config byte pointer (delay slot)
    mov.w @(6, r4), r0                  ! r0 = word[3] from scroll config A
    mov r0, r2                          ! r2 = word[3]
    extu.w r2, r2                       ! zero-extend to 32 bits
    and r14, r2                         ! keep only byte 0 (mask 0x00FF)
    extu.w r2, r0                       ! zero-extend result
    mov.w r0, @(6, r4)                  ! write back masked word[3]
    mov r6, r0                          ! r0 = display value
    mov.w   .L_bitfield_pos_count, r1       ! r1 = 0x0401 (bit position | count)
    mov.l   .L_pool_fn_bitfield_commit, r3 ! r3 = bitfield read-modify-write fn
    jsr @r3                             ! call bitfield commit
    mov r5, r2                          ! r2 = config byte pointer (delay slot)
    lds.l @r15+, pr                     ! restore return address
    rts                                 ! return
    mov.l @r15+, r14                    ! restore r14 (delay slot)
.L_bitfield_pos_count:
    .2byte  0x0401                         /* [MEDIUM] bit position (4) | count (1) for bitfield commit */


    .global DAT_0603d5be
DAT_0603d5be:
    .2byte  0x00FF
.L_mask_byte1:
    .4byte  0x0000FF00                  /* byte 1 mask */
.L_pool_config_byte_ptr:
    .4byte  sym_060A4D58
.L_pool_scroll_cfg_a:
    .4byte  sym_060A4D3E
.L_pool_fn_bitfield_commit:
    .4byte  sym_06034F78
