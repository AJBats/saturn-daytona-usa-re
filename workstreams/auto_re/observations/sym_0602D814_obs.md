---
function: sym_0602D814
address: 0x0602D814
address_end: 0x0602D88C
source_file: reimpl/src/FUN_0602D43C.s (line 600+)
explored: 2026-03-15
scenarios_tested: [straight_throttle, idle, brake]
reachable: true
---

## Call Frequency

| Scenario | Calls/Frame | Notes |
|----------|-------------|-------|
| straight_throttle | 1 | Pipeline call 18 in FUN_0602EEB8 |
| brake | 1 | Same |

## Field Access (from Static Analysis — Complete)

### Reads

| Offset | Line | Notes |
|--------|------|-------|
| +0x0C | 603 | Speed magnitude (current). `mov.l @(12, r0), r4` |
| +0xFC | 605 | Accel delta. `mov.l @(r0, r2), r3` where r2=0xFC |
| +0xDC | 618 | Section index (16-bit). `mov.w @(r0, r2), r4` where r2=0xDC |

### Writes

| Offset | Line | Notes |
|--------|------|-------|
| +0x0C | 607, 611 | `speed += accel_delta`. Floor at 0 (line 611) |
| +0xFC | 612 | Cleared to 0 when speed floors (line 612) |
| +0xE0 | 667 | Gear-clamped speed output. Clamped to [0, 0x2134] |
| +0xE8 | 674 | Speed headroom = max(0, unclamped - clamped) |

## Computation (Complete Disassembly)

```
speed = car[+0x0C] + car[+0xFC]          // integrate accel delta
if speed < 0:
    car[+0x0C] = 0
    car[+0xFC] = 0                        // clear delta when floored
else:
    car[+0x0C] = speed

gear_ratio = table[car[+0xDC]]           // sym_060477BC[section_index]
raw = (speed × gear_ratio) >> 16         // 16.16 fixed-point multiply
scaled = (raw × 0x0221AC91) >> 16        // second scaling
clamped = clamp(scaled >> 16, 0, 0x2134) // clamp to [0, 8500]
car[+0xE0] = clamped
car[+0xE8] = max(0, scaled_before_clamp - clamped)  // headroom
```

### Constants

| Value | Role |
|-------|------|
| sym_060477BC | Gear ratio lookup table, indexed by car[+0xDC] × 4 |
| 0x0221AC91 | Fixed-point scaling constant for gear→force conversion |
| 0x2134 (8500) | Maximum value for car[+0xE0] (force output clamp) |

## Feedback Loop (Empirically Confirmed)

This function completes the speed→force feedback loop:

```
Frame N:
  FUN_0602CA84 (call 15) → car[+0xFC] = force_accumulator(...)
  sym_0602D814 (call 18) → car[+0x0C] += car[+0xFC]
                          → car[+0xE0] = gear_scaled_speed (clamped to 8500)

Frame N+1:
  FUN_0602CCEC (within call 15) → car[+0x110] = f(8500 - car[+0xE0])
      (force deficit: how far from max gear speed)
  FUN_0602CA84 (call 15) → car[+0xFC] = f(..., car[+0x110], ...)
```

As speed approaches the gear-limited maximum, car[+0xE0] approaches 8500,
the force deficit (+0x110) shrinks, and the accel delta converges to zero.
This is the speed convergence mechanism.

## Per-Frame Field Verification

From CSV captures:

| Field | Throttle f0 | Throttle f299 | Brake f0 | Brake f299 | Idle |
|-------|-------------|---------------|----------|-----------|------|
| +0x0C | 0 | 43110 (29 mph) | 39574 (27 mph) | 0 | 0 |
| +0xFC | 0 | -66 | -68 | 0 | 0 |
| +0xE0 | 0 | 3308 | 3036 | 0 | 0 |
| +0xE8 | 0 | 0 | 0 | 0 | 0 |

+0xE8 (headroom) is always 0 in these captures — the clamped value never
exceeds the unclamped value at these speeds. Headroom would only appear
at high speeds where clamping actually triggers.
