function: FUN_0602D43C
address: 0x0602D43C
address_end: 0x0602D814
source_file: reimpl/src/FUN_0602D43C.s

# Largest pipeline function (689 lines). Collision response + steering angle.
# Pipeline call 16a (conditional: +0x9E == 0). Contains FUN_0602D7E4 (damping).
# Heading correction gates on speed > 240, angular velocity nonzero.

claims:
  - id: call_count_throttle
    type: call_count_per_frame
    description: "FUN_0602D43C (collision/steering) fires ~1x per frame (pipeline call 16a)"
    function: FUN_0602D43C
    address: 0x0602D43C
    scenario: straight_throttle
    expected_count: 2
    tolerance: 1

  - id: speed_increases_throttle
    type: value_changes_with_input
    description: "Speed (car[+0x0C]) increases when C is held — pipeline validation"
    address: 0x0607890C
    input: C
    direction: increases
    frames: 60

  - id: position_decreases_throttle
    type: value_changes_with_input
    description: "Position X (car[+0x10]) decreases when C is held — pipeline validation"
    address: 0x06078910
    input: C
    direction: decreases
    frames: 60
