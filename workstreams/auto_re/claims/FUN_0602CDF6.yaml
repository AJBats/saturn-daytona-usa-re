function: FUN_0602CDF6
address: 0x0602CDF6
address_end: 0x0602D088
source_file: reimpl/src/FUN_0602CDF6.s

# State finalize — heading/slip angle updates, collision counters.
# Pipeline call 17. Contains heading writer code region 0x0602CE4E.

claims:
  - id: call_count_throttle
    type: call_count_per_frame
    description: "FUN_0602CDF6 (state finalize) fires ~1x per frame (pipeline call 17)"
    function: FUN_0602CDF6
    address: 0x0602CDF6
    scenario: straight_throttle
    expected_count: 2
    tolerance: 1

  - id: speed_increases_throttle
    type: value_changes_with_input
    description: "Speed (car[+0x0C]) increases when C is held — pipeline validation"
    address: 0x0607890C
    input: C
    direction: increases
    frames: 60

  - id: position_decreases_throttle
    type: value_changes_with_input
    description: "Position X (car[+0x10]) decreases when C is held — pipeline validation"
    address: 0x06078910
    input: C
    direction: decreases
    frames: 60
