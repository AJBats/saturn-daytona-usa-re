function: FUN_0600CD40
address: 0x0600CD40
address_end: 0x0600CDCE
source_file: reimpl/src/FUN_0600CD40.s

# Track segment system — OUTSIDE player pipeline. Called from shared
# track code (0x0600CE82), runs per-car. Reads +0x10 (position),
# writes +0x184/+0x1E4 (track segment data/index).

claims:
  - id: call_count_throttle
    type: call_count_per_frame
    description: "FUN_0600CD40 (track segments) fires ~1x per frame from shared track system"
    function: FUN_0600CD40
    address: 0x0600CD40
    scenario: straight_throttle
    expected_count: 2
    tolerance: 1

  - id: speed_increases_throttle
    type: value_changes_with_input
    description: "Speed (car[+0x0C]) increases when C is held — pipeline validation"
    address: 0x0607890C
    input: C
    direction: increases
    frames: 60

  - id: position_decreases_throttle
    type: value_changes_with_input
    description: "Position X (car[+0x10]) decreases when C is held — position feeds segment advance"
    address: 0x06078910
    input: C
    direction: decreases
    frames: 60
