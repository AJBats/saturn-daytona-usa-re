function: sym_0602F7BC
address: 0x0602F7BC
address_end: 0x0602F7E2
source_file: reimpl/src/FUN_0602F5B6.s

# Timer decrementor, pipeline call 5. 12 instructions total.
# Decrements +0x166 (16-bit), +0x152 (16-bit), +0x208 (32-bit) each frame.
# All writes invisible to watchpoints (16-bit or indexed addressing).

claims:
  - id: call_count_throttle
    type: call_count_per_frame
    description: "sym_0602F7BC (timer decrementor) fires ~1x per frame (pipeline call 5)"
    function: sym_0602F7BC
    address: 0x0602F7BC
    scenario: straight_throttle
    expected_count: 2
    tolerance: 1

  - id: speed_increases_throttle
    type: value_changes_with_input
    description: "Speed (car[+0x0C]) increases when C is held — pipeline validation"
    address: 0x0607890C
    input: C
    direction: increases
    frames: 60

  - id: position_decreases_throttle
    type: value_changes_with_input
    description: "Position X (car[+0x10]) decreases when C is held — pipeline validation"
    address: 0x06078910
    input: C
    direction: decreases
    frames: 60
