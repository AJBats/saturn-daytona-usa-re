function: sym_0602F4B4
address: 0x0602F4B4
address_end: 0x0602F5B6
source_file: reimpl/src/FUN_0602F270.s

# Opponent proximity detection, pipeline call 10. Manhattan-ish distance
# check against all opponents. Writes +0xD6 = 20 on detection (16-bit).
# In time trial: no opponents, loop has 0 iterations, returns quickly.

claims:
  - id: call_count_throttle
    type: call_count_per_frame
    description: "sym_0602F4B4 (proximity detection) fires ~1x per frame (pipeline call 10)"
    function: sym_0602F4B4
    address: 0x0602F4B4
    scenario: straight_throttle
    expected_count: 2
    tolerance: 1

  - id: speed_increases_throttle
    type: value_changes_with_input
    description: "Speed (car[+0x0C]) increases when C is held — pipeline validation"
    address: 0x0607890C
    input: C
    direction: increases
    frames: 60

  - id: position_decreases_throttle
    type: value_changes_with_input
    description: "Position X (car[+0x10]) decreases when C is held — pipeline validation"
    address: 0x06078910
    input: C
    direction: decreases
    frames: 60
