function: FUN_0602C690
address: 0x0602C690
address_end: 0x0602C8E0
source_file: reimpl/src/FUN_0602C690.s

# Force magnitude computation, pipeline call 13. Combines orientation,
# track angles, roll projection → writes +0x140/+0x144 (world coords).
# All writes use indexed addressing [WP:indexed].

claims:
  - id: call_count_throttle
    type: call_count_per_frame
    description: "FUN_0602C690 (force magnitude) fires ~1x per frame (pipeline call 13)"
    function: FUN_0602C690
    address: 0x0602C690
    scenario: straight_throttle
    expected_count: 2
    tolerance: 1

  - id: speed_increases_throttle
    type: value_changes_with_input
    description: "Speed (car[+0x0C]) increases when C is held — pipeline validation"
    address: 0x0607890C
    input: C
    direction: increases
    frames: 60

  - id: position_decreases_throttle
    type: value_changes_with_input
    description: "Position X (car[+0x10]) decreases when C is held — pipeline validation"
    address: 0x06078910
    input: C
    direction: decreases
    frames: 60
